# CASCADE Propagation Platform — Backend Dependencies
#
# Install:  pip install -r requirements.txt
# Dev/test: pip install -r requirements.txt -r requirements-dev.txt
#
# Python 3.11+ required.

# ---------------------------------------------------------------------------
# Web framework
# ---------------------------------------------------------------------------

fastapi==0.136.1
starlette==1.2.0              # imported directly in main.py (BaseHTTPMiddleware) — not just a fastapi transitive dep
uvicorn[standard]==0.46.0    # [standard] adds websockets + watchfiles for --reload
python-multipart==0.0.32     # required for form/file uploads in FastAPI

# ---------------------------------------------------------------------------
# Data validation (schema-first — single source of truth)
# ---------------------------------------------------------------------------

pydantic==2.13.4
pydantic-settings==2.14.2    # Settings class with env-var loading

# ---------------------------------------------------------------------------
# Database (PostgreSQL)
# ---------------------------------------------------------------------------

asyncpg==0.31.0              # async PostgreSQL driver (used by FastAPI routes)
psycopg2-binary==2.9.12      # sync driver — used only in scripts/ and migrations

# ---------------------------------------------------------------------------
# Authentication (OAuth2 / OIDC + JWT)
# ---------------------------------------------------------------------------

python-jose[cryptography]==3.5.0   # JWT encode/decode; [cryptography] adds RS256
httpx==0.28.1                      # async HTTP — fetches OIDC discovery documents

# ---------------------------------------------------------------------------
# Graph algorithms (open, auditable core — not the engine)
# ---------------------------------------------------------------------------

networkx==3.6.1              # centrality, clustering, and topological analysis

# ---------------------------------------------------------------------------
# Network importers (EPANET .inp → CASCADE, core/inp_*.py)
# ---------------------------------------------------------------------------

wntr==1.5.0                  # EPANET parse, skeletonization, PDD hydraulics (BSD-3)
pyproj==3.7.2                # CRS transform for georeferenced imports (MIT)

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------

python-dotenv==1.2.2         # load .env files in development

# ---------------------------------------------------------------------------
# Observability
# ---------------------------------------------------------------------------

sentry-sdk[fastapi]==2.64.0                 # error tracking (Sentry/GlitchTip); enabled via SENTRY_DSN
prometheus-fastapi-instrumentator==8.0.2    # /metrics endpoint for Prometheus scraping
